{
  "name": "hyd-full-app",
  "version": "1.0.0",
  "description": "Full-stack Hyd application with Angular frontend and Express backend",
  "scripts": {
    "build": "cd hyd && npm install && NODE_OPTIONS=\"--max-old-space-size=512\" npx ng build && cd ../server && npm install && cd ..",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
